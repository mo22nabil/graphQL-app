const auhtSchema = require("./auth/auth.schema")
const rootSchema = require("./root/root.schema")
const userSchema = require("./user/user.schema")



module.exports  = {
    auhtSchema,
    rootSchema,
    userSchema
}